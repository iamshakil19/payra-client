import React from 'react';
import Header from '../Header/Header';

const Login = () => {
    return (
        <div>
            <Header/>
            <h2>This is login page</h2>
        </div>
    );
};

export default Login;