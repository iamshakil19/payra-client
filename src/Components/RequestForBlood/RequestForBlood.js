import React from 'react';
import Header from '../Header/Header';

const RequestForBlood = () => {
    return (
        <div>
            <Header></Header>
            <h2>This is requestForBlood page</h2>
        </div>
    );
};

export default RequestForBlood;